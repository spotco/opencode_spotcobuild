Ask the user questions during execution (preferences, clarifications, choices).

- With `custom` enabled (default), an "Other"/type-your-own option is added — do not invent one.
- Answers are label arrays; set `multiple: true` for multi-select.
- Put the recommended option first and append "(Recommended)" to its label.
