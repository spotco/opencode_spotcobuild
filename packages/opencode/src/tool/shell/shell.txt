${intro} OS: ${os}. Shell: ${shell}.

${workdirSection} Temp (pre-approved): `${tmp}`.

Terminal ops (git/npm/docker). Prefer dedicated file tools for read/write/edit/search.

${commandSection}

# Git / GitHub
- Commit/amend/push/PR only when asked. Before commit: status, diff, recent log; no secrets; match repo style.
- Do not change git config, skip hooks, use `-i`, force-push, or empty commits unless asked. On hook failure: new commit, do not amend.
- Use `gh` for GitHub; return PR URL.
