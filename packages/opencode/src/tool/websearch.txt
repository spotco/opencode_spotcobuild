Web search via the session provider (live results; optional URL crawl). Use for current events and post-cutoff facts.

- Crawl modes when available: `fallback` | `preferred`. Search types: `auto` | `fast` | `deep`. Domain filters may apply.
- Current year is {{year}} — include it in queries for recent info (e.g. "AI news {{year}}", not the prior year).
